/vendor/
node_modules/
npm-debug.log
yarn-error.log

# Laravel 4 specific
bootstrap/compiled.php
app/storage/

# Laravel 5 & Lumen specific
public/storage
public/hot

# Laravel 5 & Lumen specific with changed public path
public_html/storage
public_html/hot

storage/*.key
.env
Homestead.yaml
Homestead.json
/.vagrant
.phpunit.result.cache

/public/build
/storage/pail
.env.backup
.env.production
.phpactor.json
auth.json

# IDE / editors
/.idea/
/.vscode/
/.fleet/
/.nova/
/.zed/
*.swp
*.swo
*~

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Database dumps — contain real customer PII (names, phones, FCM tokens).
# Never commit these to the repo (it deploys to prod and lives on GitHub).
docs/database/*.sql
*.sql.gz
/*_dump*.sql

# AI tooling runtime/state (local — not project config)
/.claude-flow/
/.hive-mind/
/.swarm/
/.ruflo/
/memory/claude-flow-data.json

# claude-flow / ruflo local tooling — agents, commands, helpers, skills, and the
# MCP server definition. Not EVOTECH project source; kept local so the server
# checkout stays clean on pull.
/.claude/
/.mcp.json
