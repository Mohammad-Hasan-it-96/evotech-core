<?php

/*
 * App-level API routes.
 *
 * Feature endpoints live in their modules (modules/<Module>/Routes/api.php),
 * registered via each module's service provider. This file exists mainly to
 * enable Laravel's configured "api" middleware group (throttling + binding
 * substitution) for the whole API surface. Keep it thin.
 */
